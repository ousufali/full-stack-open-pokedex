name: testNotification
on: 
  push:
    branches: 
      - master
  pull_request:
    branches: [master]
    types:  [opened , synchronize]


          
  notification:
    runs-on: ubuntu-18.04s
    steps:
      - uses: actions/checkout@v2
      - name: consoling ...
        env:
          JOB:  ${{toJson(job)}}

      - uses: 8398a7/action-slack@v3
        with:
          status: ${{job.status}}
          fields: all
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL}}
        if: always()
    



        

        
